<!-- 
DO NOT EDIT THIS FILE DIRECTLY.
This file is auto-generated from docs/data/failure_scenarios.yaml.
Run `python3 scripts/generate_failure_docs.py` to update.
Generated at: 2025-11-29T17:30:47.304949
-->

# Comprehensive Failure Scenarios Catalog

This document serves as a master catalog of failure scenarios for generating training data. It covers various architectural patterns, system layers, and common failure modes found in modern distributed systems.

## 1. API & Web Services

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `API-001` | **Healthy Baseline** | Normal operation. | Stable latency/throughput. | N/A |
| `API-002` | **Latency Spike (Tail)** | p99 latency increases significantly. | Spike in p99, stable p50. | GC pauses, noisy neighbors. |
| `API-003` | **Global Latency Shift** | All requests slow down. | p50, p95, p99 all rise. | Resource saturation, code regression. |
| `API-004` | **Error Spike (5xx)** | Sudden burst of server errors. | High `5xx` rate. | Bug, downstream failure. |
| `API-005` | **Rate Limiting (429)** | Client exceeds quota. | High `429` rate. | Traffic surge, bad client. |
| `API-006` | **Bimodal Latency** | Fast and slow path distribution. | Double-hump histogram. | Cache misses, cold starts. |
| `API-007` | **Memory Leak** | Gradual memory growth. | RAM usage slope up. | Unreleased objects. |
| `API-008` | **CPU Saturation** | CPU hits 100%. | High CPU, latency spike. | Infinite loop, crypto mining. |
| `API-009` | **Thread Starvation** | No threads for new requests. | Latency spike, timeouts. | Blocking I/O. |
| `API-010` | **Large Payload** | Processing huge bodies. | Network I/O spike, latency. | Missing pagination. |
| `API-015` | **Header Injection Attack** | Malformed or malicious request headers cause downstream errors. | Spike in 4xx/5xx, request parsing errors. | Missing validation, insecure libraries. |
| `API-016` | **Payload Validation Failure** | Large or malformed JSON/XML bodies cause parsing crashes. | 400 errors, increased latency. | Schema mismatch, missing schema validation. |
| `API-011` | **GraphQL N+1 Problem** | Excessive nested queries. | DB query explosion, latency. | Missing dataloader. |
| `API-012` | **gRPC Stream Stall** | Bidirectional stream hangs. | Client timeout, no data flow. | Network issue, deadlock. |
| `API-013` | **Long Polling Timeout** | Timeout before data arrives. | Empty responses, retries. | Timeout < event interval. |
| `API-014` | **SSE Connection Drop** | Server-Sent Events disconnect. | Client reconnect storm. | Proxy timeout, network. |

## 2. Microservices & Distributed Patterns

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `SVC-001` | **Retry Storm** | Cascading retries amplify load. | Exponential request rate. | Aggressive retry policy. |
| `SVC-002` | **Circuit Breaker Open** | Fast failure after threshold. | Instant 503s/errors. | Downstream outage. |
| `SVC-003` | **Service Discovery Fail** | Cannot resolve service IP. | DNS errors, connection fail. | CoreDNS/Consul issue. |
| `SVC-004` | **Cascading Failure** | One service kills others. | Chain reaction of errors. | No bulkheads/timeouts. |
| `SVC-005` | **Clock Skew** | Time mismatch between nodes. | Auth failures, log order mess. | NTP failure. |
| `SVC-006` | **Distributed Deadlock** | Circular wait across services. | Timeouts in multiple apps. | Circular dependency. |
| `SVC-007` | **Service Mesh Misconfiguration** | Sidecar proxy (e.g., Envoy) misroutes traffic. | Unexpected latency, 502/504 errors. | Incorrect routing rules, version skew. |
| `SVC-008` | **Message Serialization Error** | Incompatible protobuf/Avro versions break communication. | High error rate, request drops. | Version drift, schema evolution issues. |

## 3. Event-Driven & Streaming (Kafka/RabbitMQ)

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `MSG-001` | **Consumer Lag** | Consumption slower than production. | Increasing lag metric. | Slow consumer, high load. |
| `MSG-002` | **Poison Pill** | Message crashes consumer. | Consumer restart loop. | Malformed data. |
| `MSG-003` | **Broker Failure** | Kafka broker down. | Producer/Consumer errors. | Infrastructure crash. |
| `MSG-004` | **Topic Imbalance** | One partition overloaded. | Uneven consumer load. | Bad partition key. |
| `MSG-005` | **Message Loss** | Data acknowledged but lost. | Missing sequence numbers. | Disk failure + bad config. |
| `MSG-006` | **Thundering Herd** | Many consumers wake at once. | CPU spike on wake. | Mass notification. |
| `MSG-007` | **Consumer Group Rebalance Storm** | Frequent rebalances cause processing pauses. | Sudden drop in throughput, lag spikes. | High churn of consumer instances. |
| `MSG-008` | **DLQ Overflow** | Dead Letter Queue filling up. | Storage growth, alert firing. | Persistent processing failures. |

## 4. Caching Layer (Redis/Memcached)

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `CSH-001` | **Cache Stampede** | Mass re-computation on expiry. | DB load spike, latency. | Simultaneous expiry. |
| `CSH-002` | **Cache Penetration** | Querying non-existent keys. | High DB load, 0% hit rate. | Malicious traffic. |
| `CSH-003` | **Cache Avalanche** | Large % of keys expire at once. | Massive DB load spike. | No jitter on TTL. |
| `CSH-004` | **Hot Key** | One key gets all traffic. | Single node CPU spike. | Viral content. |
| `CSH-005` | **Eviction Storm** | Cache full, evicting active keys. | High eviction rate. | Memory undersized. |
| `CSH-006` | **Cache Invalidation Race** | Stale data served after update. | Inconsistent read results, cache hit but stale. | Poor invalidation strategy. |

## 5. Database & Storage (SQL/NoSQL)

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `DB-001` | **Slow Query** | Missing index scan. | High DB latency. | Bad schema/query. |
| `DB-002` | **Connection Pool Full** | App waits for connection. | App latency, low DB CPU. | Leak, small pool. |
| `DB-003` | **Replication Lag** | Replica data is stale. | High lag metric. | Network, heavy writes. |
| `DB-004` | **Deadlock** | Txns blocking each other. | DB error count. | Locking order. |
| `DB-005` | **Disk I/O Saturation** | IOPS limit reached. | High `iowait`. | Noisy neighbor, backups. |
| `DB-006` | **Transaction Log Full** | Cannot write new data. | Write failures. | Disk full, archiving fail. |
| `DB-007` | **Schema Drift / Migration Conflict** | Two services use different schema versions. | Query failures, data corruption. | Out-of-sync migrations. |
| `DB-008` | **Transaction ID Wraparound** | DB stops accepting writes to prevent data loss. | Hard stop on writes, emergency vacuum. | Vacuum process failure/lag. |
| `DB-009` | **Integer Overflow** | Primary key sequence limit reached. | Insert failures. | Using INT instead of BIGINT. |

## 6. Infrastructure & Kubernetes

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `INF-001` | **OOMKill** | Container killed by kernel. | Restart count +1. | Memory leak. |
| `INF-002` | **CPU Throttling** | CFS quota enforcement. | Throttled seconds rise. | Low limits. |
| `INF-003` | **Node Pressure** | Node resources exhausted. | Pod evictions. | Over-provisioning. |
| `INF-004` | **Packet Loss** | Network drops. | Retransmits, timeouts. | CNI/Hardware issue. |
| `INF-005` | **DNS Latency** | Slow name resolution. | Connection setup lag. | CoreDNS overload. |
| `INF-006` | **Ephemeral Storage** | Disk full on node. | Pod eviction. | Log spam. |
| `INF-007` | **Pod Disruption Budget Violation** | Too many pods evicted during maintenance. | Unexpected pod restarts, service degradation. | Mis-configured PDB. |
| `INF-008` | **Node Drain Timeout** | Node draining hangs, pods stay pending. | High pod pending count, scheduling delays. | Stuck daemonset or finalizer. |
| `INF-009` | **Image Pull BackOff** | Container fails to start. | ImagePullBackOff status. | Registry rate limit, auth fail. |
| `INF-010` | **Init Container Failure** | Pod stuck in Init state. | CrashLoopBackOff (Init). | Dependency check fail, setup script error. |
| `INF-011` | **Liveness Probe Loop** | Pod restarts repeatedly after startup. | High restart count, short uptime. | Probe timeout < startup time. |

## 7. Serverless (FaaS)

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `SRV-001` | **Cold Start** | Container init latency. | High initial latency. | Idle timeout. |
| `SRV-002` | **Function Timeout** | Execution exceeds limit. | Error/Timeout metric. | Slow dependency. |
| `SRV-003` | **Concurrency Limit** | Throttled invocations. | 429 errors. | Burst traffic. |
| `SRV-004` | **Event Source Lag** | Trigger processing delay. | Iterator age increase. | Stream backlog. |

## 8. AI/ML Inference (Specialized)

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `GPU-001` | **VRAM Saturation** | OOM during inference. | GPU mem 100%, crash. | Batch size too big. |
| `GPU-002` | **Thermal Throttling** | GPU slows down. | Clock speed drop. | Overheating. |
| `GPU-003` | **PCIe Bottleneck** | Slow data transfer. | GPU idle, high bus use. | Large inputs. |
| `GPU-004` | **Token Latency** | Slow generation per token. | High TTFT/TPOT. | Compute bound. |

## 9. Security & Edge

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `SEC-001` | **DDoS (Volumetric)** | Bandwidth saturation. | Network in/out spike. | Attack. |
| `SEC-002` | **Credential Expiry** | Auth failures. | 401/403 spike. | Token rotation fail. |
| `SEC-003` | **Bad Bot Traffic** | Scraping/Scanning. | 404s, weird paths. | Scrapers. |
| `SEC-004` | **Ransomware / Data Corruption** | Files encrypted or corrupted by malware. | Sudden I/O errors, file read failures. | Compromise, lack of backups. |
| `SEC-005` | **Supply-Chain Compromise** | Third-party library introduces vulnerability. | Unexpected crashes, security alerts. | Unvetted dependency update. |
| `SEC-006` | **JWT Algorithm Confusion** | Auth bypass via None algorithm. | Unauthorized access success. | Insecure JWT library config. |

## 10. Load Balancer & Reverse Proxy

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `LB-001` | **Unhealthy Backend Pool** | All backends marked down. | 503 errors, no traffic to backends. | Health check misconfiguration. |
| `LB-002` | **Sticky Session Failure** | Session affinity breaks. | Random backend routing. | LB config change, backend restart. |
| `LB-003` | **SSL/TLS Handshake Fail** | Certificate validation errors. | Connection refused, timeout. | Expired cert, cipher mismatch. |
| `LB-004` | **WebSocket Upgrade Fail** | Unable to upgrade connection. | 426 Upgrade Required errors. | Proxy timeout config. |
| `LB-005` | **Header Size Limit** | Request headers too large. | 431 errors. | Too many cookies, long headers. |

## 11. Network Layer (Lower Level)

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `NET-001` | **MTU Mismatch** | Packet fragmentation issues. | Intermittent timeouts, packet loss. | Network config mismatch. |
| `NET-002` | **ARP Table Exhaustion** | Cannot resolve MAC addresses. | Connection failures. | Too many hosts. |
| `NET-003` | **TCP SYN Flood** | SYN queue overflow. | Connection refused, high drop rate. | DDoS attack. |
| `NET-004` | **Connection Timeout** | TCP handshake timeout. | Latency spike, timeouts. | Firewall, routing issue. |
| `NET-005` | **Half-Open Connections** | Incomplete TCP handshake. | Socket exhaustion. | Network instability. |
| `NET-006` | **Network Partition** | Split-brain scenario. | Inconsistent state, quorum loss. | Link failure. |
| `NET-007` | **BGP Flap / Route Instability** | Frequent route changes cause packet loss. | Increased latency, retransmissions. | Mis-configured BGP peers. |
| `NET-008` | **DNS Cache Poisoning** | Stale or malicious DNS entries. | Wrong service endpoints, connection failures. | Compromised DNS resolver. |

## 12. Storage & Volume

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `STO-001` | **PV/PVC Mount Failure** | Volume fails to mount. | Pod CrashLoopBackOff. | CSI driver issue, permissions. |
| `STO-002` | **Read-Only Filesystem** | Cannot write to disk. | Write errors, app crash. | Disk corruption, kernel panic. |
| `STO-003` | **Quota Exceeded** | Disk quota limit reached. | Write failures. | Oversized data. |
| `STO-004` | **Snapshot in Progress** | Performance degraded during backup. | High I/O latency. | Snapshot/backup operation. |
| `STO-005` | **Cold Data Tiering Latency** | Data moved to cold tier causes slow reads. | High read latency spikes. | Tiering policy mis-tuned. |
| `STO-006` | **Filesystem Corruption** | Corrupt inode tables cause read errors. | I/O errors, data loss. | Disk failure, abrupt power loss. |

## 13. Resource Exhaustion (Granular)

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `RES-001` | **File Descriptor Limit** | Out of FDs. | "Too many open files" errors. | Leak, insufficient limit. |
| `RES-002` | **Inode Exhaustion** | No inodes available. | Cannot create files. | Many small files. |
| `RES-003` | **Port Exhaustion** | Ephemeral ports depleted. | Connection errors. | High connection churn. |
| `RES-004` | **Memory Fragmentation** | Available but unusable memory. | Allocation failures despite free RAM. | Long-running process. |
| `RES-005` | **Semaphore Exhaustion** | IPC limits reached. | App hang, deadlock. | Too many processes. |
| `RES-006` | **GPU Memory Fragmentation** | VRAM fragmented causing OOM despite free space. | Intermittent OOM, inference failures. | Long-running inference jobs. |

## 14. Observability & Monitoring

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `OBS-001` | **Metrics Scrape Timeout** | Prometheus cannot scrape. | Missing metrics, stale data. | App overload, network issue. |
| `OBS-002` | **Log Shipper Backpressure** | Logs buffering/dropping. | Incomplete logs. | Log volume spike. |
| `OBS-003` | **Trace Sampling Issues** | Missing critical traces. | Incomplete observability. | Low sampling rate. |
| `OBS-004` | **Dashboard Query Timeout** | Grafana queries fail. | Empty panels, errors. | Heavy queries, DB overload. |
| `OBS-005` | **Alert Fatigue / Noise** | Too many alerts fire for benign spikes. | Alert rate explosion, high false-positive ratio. | Low threshold, missing deduplication. |

## 15. Configuration & Deployment

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `CFG-001` | **Config Reload Failure** | App fails to load new config. | Restart loop, old config persists. | Syntax error, validation fail. |
| `CFG-002` | **Feature Flag Issue** | Flag flip causes breakage. | Error spike post-deployment. | Untested code path. |
| `CFG-003` | **Canary Stuck** | Canary deployment halted. | Partial traffic to new version. | Metrics below threshold. |
| `CFG-004` | **Schema Migration Fail** | Database migration error. | App errors, inconsistent state. | Locking, syntax error. |
| `CFG-005` | **Environment Mismatch** | Wrong env vars loaded. | Config errors, wrong DB. | Deployment automation bug. |
| `CFG-006` | **Secret Leakage** | Sensitive credentials exposed in logs/config. | Unexpected auth failures, security alerts. | Logging of env vars, mis-scoped secrets. |

## 16. Cloud Provider & Control Plane

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `CLD-001` | **DNS Control Plane Race** | DNS records deleted during management race condition. | Empty DNS responses, cascading service failures. | Race in DNS management system. |
| `CLD-002` | **Service Control Quota Crash** | Malformed quota policy crashes global service control. | Null pointer exceptions, global outage. | Blank field in policy, Spanner replication. |
| `CLD-003` | **DDoS Protection Inversion** | DDoS protection amplifies attack traffic. | Traffic amplification instead of absorption. | Bug in protection layer logic. |
| `CLD-004` | **IAM Propagation Delay** | Eventually consistent IAM causes sporadic auth failures. | AccessDenied errors after IAM changes. | Cross-region IAM replication lag. |

## 17. Container Runtime & Sidecars

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `CTR-001` | **Containerd Shim Process Leak** | Orphaned shim processes accumulate. | Process count grows beyond container count. | UnmountAll timeout, gRPC deadline. |
| `CTR-002` | **Sidecar Startup Race** | App starts before sidecar proxy ready. | ECONNREFUSED, traffic bypass. | CNI timing, iptables not applied. |
| `CTR-003` | **Sidecar Memory Bloat** | Envoy sidecars consume 1GB+ memory. | OOMKill, high memory per pod. | Full service mesh config push. |
| `CTR-004` | **Native Sidecar Non-Termination** | Pods hang in Terminating state indefinitely. | Stuck pod deletion. | Sidecar never ready before deletion. |

## 18. Certificate & PKI

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `PKI-001` | **Certificate Chain Validation Fail** | Valid leaf cert rejected due to chain issues. | "Certificate expired" on legacy clients. | Cross-sign expiration, old OpenSSL. |
| `PKI-002` | **mTLS Health Check Failure** | Kubelet probes fail under strict mTLS. | Liveness/readiness probe failures. | Kubelet lacks mTLS certs. |
| `PKI-003` | **SPIRE Agent SVID Failure** | Workload identity verification fails. | Envoy SDS errors, listener resets. | Cgroup issues, trust bundle expiry. |

## 19. Autoscaling & Operators

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `ASC-001` | **HPA Metric Staleness** | Deployment causes unnecessary scale-up. | Replica increase during rollout. | New pods increase count before metrics. |
| `ASC-002` | **Cluster Autoscaler Delay** | 15+ minute provisioning delays. | Pods pending for extended time. | Initial node group failure, waiting timeout. |
| `ASC-003` | **CRD Finalizer Deadlock** | CRD deletion blocks indefinitely. | InstanceDeletionInProgress state. | Finalizers prevent CR writes. |
| `ASC-004` | **Webhook Timeout Cascade** | Slow webhooks block entire control plane. | API server timeouts, pod creation blocks. | Webhook service unavailable. |

## 20. Object Storage

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `OBJ-001` | **S3 Request Rate Limiting** | 503 SlowDown errors. | Exceeded 3.5K PUT or 5.5K GET/sec per prefix. | Partition key patterns, no warmup. |
| `OBJ-002` | **Lifecycle Policy Misfire** | Premature object deletion. | Unexpected object count drops. | Overlapping rules, timezone mismatch. |
| `OBJ-003` | **Cross-Region Replication Lag** | DR failover during 48hr lag window. | Data loss on failover. | Large objects, no RTC SLA. |

## 21. CDN & Edge Computing

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `CDN-001` | **Cache Poisoning via Headers** | Unkeyed headers modify cached responses. | Stale/malicious content served globally. | Header processing differences. |
| `CDN-002` | **Edge Workers KV Failure** | Third-party storage failure cascades. | 90%+ KV failure rate. | Removed dual-provider redundancy. |
| `CDN-003` | **Rate Limiting Infinite Loop** | DDoS rule triggers handler infinite loop. | 100% CPU, permanent errors. | Lua tail-call optimization. |
| `CDN-004` | **Lambda@Edge Deployment Fail** | Cross-region replication failure. | Hours-long deployment delays. | us-east-1 requirement, replication lag. |

## 22. Time-Series Databases

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `TSDB-001` | **Prometheus Compaction Failure** | Empty chunks after deletion API. | Failed compactions, unbounded WAL. | populateWithDelChunkSeriesIterator error. |
| `TSDB-002` | **WAL Replay OOM** | Restart fails with OOM. | Crash loop on startup. | Massive WAL (63GB+) from no compaction. |
| `TSDB-003` | **Cardinality Explosion** | High-cardinality labels create millions of series. | Memory exhaustion (200GB+). | User IDs, request IDs as labels. |
| `TSDB-004` | **Thanos Block Overlap** | Compactor halts with overlap errors. | Manual intervention required. | Duplicate external labels, upload errors. |

## 23. Connection Poolers & Queues

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `POOL-001` | **PgBouncer Session State Leak** | Pool connections retain session state. | All writes fail with "read-only transaction". | server_reset_query failure. |
| `POOL-002` | **Backend Connection Storm** | Pool restart overwhelms database. | pg_stat_activity spikes to max_connections. | No connection rate limiting. |
| `POOL-003` | **SQS Visibility Timeout** | Duplicate message processing. | Same message processed multiple times. | Timeout < processing time. |
| `POOL-004` | **NATS JetStream Consumer Stall** | Consumer receives nothing after leader fail. | No data flow, no errors. | Subscription bound to old leader. |
| `POOL-005` | **Redis Streams Silent Stall** | XREADGROUP stops after 10-12 hours. | No messages, no error. | TCP connection silently breaks. |

## 24. eBPF & Kernel-Level

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `EBPF-001` | **eBPF Verifier Register Bug** | Verifier allows arbitrary kernel memory access. | Privilege escalation, container escape. | Register limit tracking bugs. |
| `EBPF-002` | **Cilium Datapath Kernel Panic** | Array-index-out-of-bounds in BPF. | Cluster bootstrap failure, kernel crash. | netkit datapath mode, kernel 6.8. |
| `EBPF-003` | **eBPF Map Memory Leak** | Kernel memory grows 200-500MB+. | Memory exhaustion. | eBPF loader process not killed. |

## 25. Distributed Tracing

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `TRC-001` | **Async Context Loss** | Trace context not propagated to async tasks. | Broken trace trees, orphaned spans. | ThreadLocal not propagated. |
| `TRC-002` | **OTel Collector Queue Drop** | Spans dropped under high load. | Memory growth, OOM. | Slow export destinations. |
| `TRC-003` | **Elasticsearch Index Rollover Fail** | Indices stuck in hot phase, grow unbounded. | Single index exceeds limits. | Alias config, name not ending in digits. |
| `TRC-004` | **SIEM License Capacity Exceeded** | Log ingestion stops when volume exceeded. | Blind spots during incidents. | Commercial SIEM volume limits. |

## 26. GitOps & Chaos Engineering

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `GITOPS-001` | **ArgoCD Resource Pruning** | Critical resources deleted by sync. | Workload replacement, data loss. | Missing Prune=false annotation. |
| `GITOPS-002` | **Litmus ChaosEngine Finalizer Stuck** | Namespace deletion blocks indefinitely. | Irrecoverable finalizer state. | Operator deleted before CRD cleanup. |
| `GITOPS-003` | **Chaos Mesh Kernel-Level Blast** | KernelChaos affects all pods on host. | Unintended blast radius. | BPF injection at kernel level. |
| `GITOPS-004` | **Network Chaos Control Plane Impact** | Broad selectors affect kube-system. | API server latency, etcd elections. | Missing namespace exclusions. |

## 27. Multi-Tenancy Isolation

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `MTN-001` | **Network Policy Gaps** | Empty podSelector selects all pods. | Cross-namespace communication allowed. | Missing policyTypes specification. |
| `MTN-002` | **Node Affinity Bypass** | Tenants access dedicated node pools. | Isolation violation. | Users can add tolerations. |
| `MTN-003` | **Shared CRD Version Conflict** | Tenants require different CRD versions. | Impossible isolation. | CRDs cannot be namespaced. |
| `MTN-004` | **Cross-Tenant DNS Resolution** | Service discovery across namespaces. | Reconnaissance of other tenants. | Default Kubernetes DNS permits all. |

## 28. Data Pipeline & CDC

| ID | Name | Description | Metrics Pattern | Root Cause |
| :--- | :--- | :--- | :--- | :--- |
| `CDC-001` | **Debezium Snapshot Interrupt** | Re-snapshot required if interrupted. | Streaming blocked, long recovery. | Binlog purged before catch-up. |
| `CDC-002` | **Debezium Memory Allocation Fail** | Large table snapshot OOM. | Snapshot restart loop. | Insufficient memory for table size. |

