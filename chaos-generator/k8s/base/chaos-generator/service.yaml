apiVersion: v1
kind: Service
metadata:
  name: chaos-generator
  namespace: heimr-chaos
  labels:
    app: chaos-generator
    component: chaos-generator
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: http
    protocol: TCP
  selector:
    app: chaos-generator
